/*
        package Lesson4;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class SelfTraining {
    public static void main(String[] args) {
        SelfTraining selfTraining = new SelfTraining();
        List<Integer> inputNumbers = Arrays.asList(1, 2, 3);
        System.out.println(selfTraining.square(inputNumbers));

        inputNumbers = Arrays.asList(6, 8, -6, -8, 1);
        System.out.println(selfTraining.square(inputNumbers));
    }

    public List<Integer> square(List<Integer> inputNumbers) {
        List<Integer> squaredList = new ArrayList<>();

        for (Integer number : inputNumbers) {
            squaredList.add(number * number);
        }

        return squaredList;
    }
}

 */