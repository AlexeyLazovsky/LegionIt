package Lesson4;
/*
public class DifferenceMaxMin {
    public static void main(String[] args) {
        DifferenceMaxMin differenceMaxMin = new DifferenceMaxMin();
        int[] numbers = {5, 7, 2, 9, 7, 1, 7, 6, 7, 7};

        int difference = differenceMaxMin.findDifference(numbers);
        System.out.println("Difference between max and min: " + difference);
    }

    int findDifference(int[] numbers) {
        int size = numbers.length;
        int max = numbers[0];
        int min = numbers[0];

        for (int i = 1; i < size; i++) {
            if (numbers[i] > max) {
                max = numbers[i];
            }

            if (numbers[i] < min) {
                min = numbers[i];
            }
        }

        return max - min;
    }
}


 */